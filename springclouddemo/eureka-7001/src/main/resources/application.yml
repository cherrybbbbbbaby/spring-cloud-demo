server:
  port: 7001
  

spring:
  application:
    name: eureka-server-culster



eureka:  instance:    instance-id: eureka-7001
    prefer-ip-address: true
  client:    service-url:
      defaultZone: http://localhost:7002/eureka         #cluster config      

info:       #info page
  discription: eureka server demo
  artifactId: $project.artifactId$
  version: $project.version$        